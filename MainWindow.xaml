<Window x:Class="LogAnalyzer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Analyzer" Height="600" Width="1000" Background="#F0F0F0">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#CCCCCC" BorderThickness="0,0,0,1" Padding="10">
            <StackPanel Orientation="Horizontal">

                <Button x:Name="BtnOpenLog" Content="Open Log" Click="BtnOpenLog_Click" 
                Padding="15,5" Margin="0,0,20,0" Background="#E1E1E1"/>

                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="TxtSearch" Width="150" VerticalAlignment="Center" 
                 Margin="0,0,20,0" TextChanged="TxtSearch_TextChanged"/>

                <TextBlock Text="Severity:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox x:Name="CmbSeverity" Width="100" SelectedIndex="0" 
                  SelectionChanged="CmbSeverity_SelectionChanged" Margin="0,0,20,0">
                    <ComboBoxItem Content="All"/>
                    <ComboBoxItem Content="Info"/>
                    <ComboBoxItem Content="Warning"/>
                    <ComboBoxItem Content="Error"/>
                    <ComboBoxItem Content="Debug"/>
                </ComboBox>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#DBEAFE" CornerRadius="5" Padding="8,2" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Total: " FontWeight="Bold" Foreground="#1E40AF"/>
                            <TextBlock x:Name="TxtTotalCount" Text="0" Foreground="#1E40AF"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FEE2E2" CornerRadius="5" Padding="8,2" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Errors: " FontWeight="Bold" Foreground="#991B1B"/>
                            <TextBlock x:Name="TxtErrorCount" Text="0" Foreground="#991B1B"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#FEF3C7" CornerRadius="5" Padding="8,2" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Warnings: " FontWeight="Bold" Foreground="#92400E"/>
                            <TextBlock x:Name="TxtWarningCount" Text="0" Foreground="#92400E"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#89C9C1" CornerRadius="5" Padding="8,2" Margin="5,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Debug: " FontWeight="Bold" Foreground="#808080"/>
                            <TextBlock x:Name="TxtDebugCount" Text="0" Foreground="#808080"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="1" x:Name="LogDataGrid" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  AlternatingRowBackground="#F9F9F9"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#EEEEEE"
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Line" Binding="{Binding LineNumber}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTemplateColumn Header="Severity" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Severity}" FontWeight="Bold" 
                                       Foreground="{Binding SeverityColor}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Severity}" Value="Error">
                            <Setter Property="Background" Value="#FFEBEE"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Warning">
                            <Setter Property="Background" Value="#FFF3E0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>
    </Grid>
</Window>